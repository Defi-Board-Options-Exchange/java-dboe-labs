dependencies {
    compile project(':utils')
    compile project(':common')

    def googleCloudPubSub = '1.108.3'
    compile group: 'com.google.cloud', name: 'google-cloud-pubsub', version: googleCloudPubSub

    testCompile project(':component-testing')
}
